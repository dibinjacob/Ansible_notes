---
plugin: community.vmware.vmware_vm_inventory
strict: false  # Use lowercase for boolean values
hostname: 192.168.52.150
username: administrator@vsphere.local
password: P@ssw0rd
validate_certs: false  # Use lowercase for boolean values
gather_subset:
  - all
